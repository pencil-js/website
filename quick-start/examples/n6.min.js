import { Scene, Circle, Rectangle } from "https://unpkg.com/pencil.js/dist/pencil.esm.js";

const scene = new Scene(undefined, {
    fill: "#45a8ff",
});
const { width, height } = scene;
// Initialize the target position
scene.cursorPosition.set(scene.center).subtract(0, height / 4);

const sun = new Circle(scene.center.add(0, height / 2), 100, {
    fill: "orange",
});

const ocean = new Rectangle([0, height / 2], width, height / 2, {
    fill: "#28488f",
});

scene
    .add(sun, ocean)
    .startLoop()
    // Listen to the "draw" event fired each frame (about 60 times a second)
    .on("draw", () => {
        // Move the sun's position towards the cursor
        sun.position.lerp(scene.cursorPosition, 0.05);
    }, true);
