import { Scene, Circle } from "https://unpkg.com/pencil.js/dist/pencil.esm.js";

const scene = new Scene();

// Create a circle shape
const radius = 100;
const circle = new Circle(scene.center, radius);

// Add it to the scene
scene.add(circle);

scene.render();
