import { Scene, Triangle } from "https://unpkg.com/pencil.js/dist/pencil.esm.js";

const scene = new Scene();

const size = 30;
const triangle = new Triangle(scene.center, size);

scene.add(triangle).render();
